# Example environment configuration file containing variables for database, encryption, and upload settings.

# Database Settings
DB_TYPE=mongodb           # mongodb | postgres
DB_URI=mongodb://localhost:27017/testdb

# Backup Settings
BACKUP_DIR=/tmp/backups
ENCRYPTION_KEY=my_secure_passphrase
CLEANUP_AFTER_UPLOAD=true

# Cloud Storage (user-specified destination)
# Example: SSH upload, rsync, SFTP, or web API endpoint
UPLOAD_METHOD=sftp
UPLOAD_HOST=backup.myserver.com
UPLOAD_USER=backupuser
UPLOAD_PATH=/remote/backup/path

# Logging
LOG_FILE=/var/log/autobackup.log
